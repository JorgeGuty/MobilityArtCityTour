import { StyleSheet } from 'react-native'
import { Colors } from '../../../constants/colors'

export const DeviceDetailStyle = StyleSheet.create({
  mainContainer: {
    width: '100%',
    height: '100%',
    flexDirection: 'column'
  },
  contentContainer: {
    width: '100%',
    height: 'auto',
    flexDirection: 'row'
  },
  borders: {
    borderWidth: 1
  }
})
